# Configuration

Staging area for configuration artifacts that will eventually correspond to
`iot-reference-rx/Configuration/`.

Planned content:
- middleware XML / MDF definitions
- sample configuration sets used by app and boot loader
- board-specific configuration selected for RX72N Envision Kit

Expected source baseline:
- `iot-reference-rx/Configuration/`
