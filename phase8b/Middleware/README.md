# Middleware

Staging area for middleware and third-party components aligned with
`iot-reference-rx/Middleware/`.

Planned groups:
- `3rdparty/`
- `AWS/`
- `FreeRTOS/`
- `logging/`
- `network_transport/`

Expected source baseline:
- `iot-reference-rx/Middleware/`

Important:
- the current repository still has legacy `libraries/`, `freertos_kernel/`, and `vendors/`
  trees, so this staging directory exists to keep the migration non-destructive
