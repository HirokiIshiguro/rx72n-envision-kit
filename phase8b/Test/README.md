# Test

Staging area for test-side assets that will eventually align with
`iot-reference-rx/Test/`.

Initial expectation:
- keep using the current repository test scripts during early migration
- only import or align test-side assets here when the new app / boot loader baseline
  becomes buildable
